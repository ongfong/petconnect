import Layout from '../../components/Layout';
import Link from 'next/link';

const TagNotRegister = () => {
    return (
        <Layout>
             <h2>The tag is not Registered yet.</h2>
        </Layout>
    );
};

export default TagNotRegister;